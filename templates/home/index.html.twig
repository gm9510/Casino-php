{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Wellcome to casino home ✅</h1>

    Casino <a href="{{ path('home') }}"> Play </a>
    <a href="{{ path('player_index') }}">back to list</a>
    <ul>
        <li>Generating Random number: {{ color }} </li>
        {% for player in players %}
        <li>{{ player.name }}</li>
        <ul>
            <li> Dinero: ${{ player.money }}</li>
            <li> Apuesta: {{ player.bet }}</li>
            <li>al Color: {{ player.color }}</li>
        </ul>
        {% else %}
        <li>No players registered</li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
